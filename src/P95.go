package main

import "fmt"

func main() {
	n := generateTrees(3)
	fmt.Println(len(n))
}

var res []*TreeNode
var record map[]
func generateTrees(n int) []*TreeNode {
	res := make([]*TreeNode, 0)

	return res
}